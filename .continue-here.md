---
session_type: gsd:debug
slug: knockout-bracket-not-populating
debug_file: .planning/debug/knockout-bracket-not-populating.md
status: paused
head_commit: f75ad30
last_updated: 2026-06-09T15:56:35.302Z
---

<current_state>
Debug session "knockout bracket does not populate from a user's group-stage predictions"
is functionally COMPLETE in code. Root causes found + fixed + verified, committed as
**f75ad30** and pushed to **origin/main**. Local CI-equivalent validation is fully green
(lint, 77/77 tests, build). What remains is OPERATIONAL and gated on explicit approval:
applying the one-time production data fix, and deploying the build. Nothing has touched any
production/real DB and nothing has been deployed.
</current_state>

<completed_work>

- Root cause 1 (PRIMARY): bracket view received no group data — FIXED in
  `app/(main)/predictions/[stage]/page.tsx` (always builds matchesByGroup). User-confirmed.
- Root cause 2: R32/R16/QF feeder placeholders were structurally wrong after the
  real-fixtures migration — FIXED. R32 (m73-88), R16 (m89-96), QF (m97-100) corrected.
- Resolver mapping verified (make-or-break): KnockoutBracket.tsx + lib/predictedStandings.ts
  key predictedWinners by matchNumber; stageOffsets {R32:72,R16:88,QF:96,SF:100} resolve
  correctly. Resolver was never the bug — only the feeder DATA was wrong.
- Full bracket verified m73-m104: 12 winners x1, 12 runners-up x1, 8 thirds (all in away
  slot), every R16/QF/SF/Final feeder references each upstream match exactly once.
- Scripts staged: `migrate-real-fixtures.js` (fresh-seed parity) +
  `fix-knockout-bracket.js` (prod overwrite by matchNumber, asserts 16/8/4).
- Committed 3 files as f75ad30, pushed to origin/main.
- Local validation: `npm ci` (repaired corrupt node_modules) -> lint PASS, test 77/77 PASS,
  build PASS. `.next` produced (73M), kept for Infomaniak upload.
</completed_work>

<remaining_work>

- Apply `fix-knockout-bracket.js` to the PRODUCTION DB (gated on approval). Existing live
  R32 data is structurally invalid (positional, zero thirds, phantom groups M-P) — the code
  fix alone does NOT repair existing rows, so this script must run once on prod.
- Deploy the validated `.next` build to Infomaniak per DEPLOYMENT-CHEATSHEET.md.
- Optional: re-run the GitHub Actions "Tests" workflow for f75ad30 once runners recover
  (the run never executed — hosted runner not acquired, GitHub infra issue, not a code fail).
</remaining_work>

<decisions_made>

- Thirds use ORDERED best-thirds (top-8, positional by matchNumber), NOT FIFA Annex C.
  Resolver only parses the "3rd " prefix; cluster labels are cosmetic. Annex C = optional
  separate task. User-approved.
- Production repair SETs the bracket by matchNumber (not regex-reformat) because the live
  data is structurally wrong. Superseded `fix-r32-placeholders.js` and `fix-r32-bracket.js`.
- SF (101-102), third-place (103), Final (104) left untouched — user confirmed correct.
- Committed only the 3 verified files; excluded superseded scripts, debug notes, CLAUDE.md.
</decisions_made>

<blockers>

- GitHub hosted runner not acquired for f75ad30 (external/GitHub infra). Worked around with
  local validation — all green.
- Production DATABASE_URL / SQLite path on Infomaniak unconfirmed — confirm before applying
  fix-knockout-bracket.js (may differ from local prisma/dev.db).
</blockers>

<context>
The resolver is correct; this was a DATA problem introduced by the real-fixtures migration.
The hard part (figuring out the valid 16-match R32 table + the R16/QF cross-bracket feeders +
proving the resolver resolves R16+) is done and verified against the official WC2026 bracket.
The only thing standing between "fixed in repo" and "fixed for users" is running
fix-knockout-bracket.js once against the live DB (after a backup) and deploying the build.
Both were explicitly held pending user approval.

Untracked/leftover: `.next/` (73M, KEEP for upload), an ephemeral CI `test.db` (safe to
delete), superseded `fix-r32-bracket.js` / `fix-r32-placeholders.js` (can delete), and the
debug notes under .planning/.
</context>

<next_action>
Start with: confirm the production DATABASE_URL/path on Infomaniak, back up the live SQLite
file (`cp <prod.db> <prod.db>.bak-$(date +%Y%m%d-%H%M%S)`), then run
`node fix-knockout-bracket.js` with the prod DATABASE_URL and re-read a few rows to confirm.
Separately, upload the `.next` build per DEPLOYMENT-CHEATSHEET.md. Full table + counts are in
`.planning/debug/knockout-bracket-not-populating.md`.
</next_action>
